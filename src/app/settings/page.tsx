import SettingsPage from '@/components/settings/SettingsPage'

export const metadata = {
  title: 'Settings — Seattle Family Bucket List',
}

export default function Page() {
  return <SettingsPage />
}
